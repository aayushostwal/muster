// Dev/default placeholder. In the Docker image, docker-entrypoint.sh
// regenerates this file from the VITE_API_URL env var before `vite preview`
// starts, so the container can point at a backend without a rebuild.
window.__MUSTER_API_URL__ = window.__MUSTER_API_URL__ || "";
